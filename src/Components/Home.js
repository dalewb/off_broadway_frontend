import React from 'react';

const Home = () => (
  <React.Fragment>
    <span id='welcome-headline'>Welcome to OFF-BROADWAY!</span>
    <span id='welcome-subhead'>The only place on the interwebs where you can write 5 lines of script and choose your own pre-determined actors to fill the roles!</span>
    <object id='group-shot' aria-label='Actor Group' data='/assets/standup.svg' type="image/svg+xml"></object>
  </React.Fragment>
)

export default Home;
